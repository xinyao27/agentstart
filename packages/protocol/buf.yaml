version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  # Why: RPCs intentionally reuse domain records instead of parallel method-specific wrappers.
  except:
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
