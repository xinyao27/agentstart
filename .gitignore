# Build artifacts
tsconfig.*.tsbuildinfo

# Why: the APNs gateway's binding is generated by `wrangler types` and must stay tracked.
!apps/apns-gateway/worker-configuration.d.ts

# Dependencies
node_modules/

# Build output
dist/
.output/
.wxt/
.prerender/
out/
build/
release/
/packages/protocol/rust/target/
/packages/protocol/swift/.build/

# Library-local resolver state; app-level lockfiles remain authoritative.
/packages/protocol/rust/Cargo.lock
/packages/protocol/swift/Package.resolved

# pnpm
.pnpm-store/
package-lock.json

# Wrangler
.wrangler/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.serena/
*.swp
*.swo
*~

# OS
*.stackdump
.DS_Store
Thumbs.db

# Lint/cache
.oxlintcache

# Logs
*.log
*.log.*
npm-debug.log*
pnpm-debug.log*

# Temp
tmp/
.tmp/
design-docs/
.context/
.atl/

# Local-only design/planning docs (not checked in).
# Durable docs must live in one of the allow-listed locations below.
docs/**
!docs/
!docs/reference/
!docs/reference/**
!docs/style-guide.md

# Local scratch notes and PR evidence screenshots (not part of the product).
/notes/
/pr-evidence/

# Agent skill installations (machine-local, populated by agent tooling)
/.claude/skills/
/.agents/skills/
/skills-lock.json

# Agent hook runtime endpoints (machine-local secrets)
/agent-hooks/
