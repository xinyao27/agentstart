{
  "name": "@agentstart/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "AgentStart runtime daemon, CLI, Native Messaging host, and host adapters",
  "scripts": {
    "build": "vp run --filter '{.}^...' build && cargo build --release --locked",
    "dev": "vp run --filter '{.}^...' build && cargo run --locked -- daemon",
    "fmt": "cargo fmt --all",
    "lint": "vp run --filter '{.}^...' build && cargo fmt --all --check && cargo clippy --locked --all-targets -- -D warnings",
    "typecheck": "vp run --filter '{.}^...' build && cargo check --locked"
  },
  "dependencies": {
    "@agentstart/protocol": "workspace:*"
  }
}
